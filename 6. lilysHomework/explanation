To sort ascending: [2, 5, 3, 1] → [1, 2, 3, 5] Requires 2 swaps.
To sort descending: [2, 5, 3, 1] → [5, 3, 2, 1] Requires 1 swap,
but since we check both, we return the minimum = 2 (depending on specific steps).
